version: "3.9"

services:
  redis:
    image: redis:latest
    container_name: redis-server
    ports:
      - "6379:6379"

  python-app:
    build:
      context: ./python-app
    container_name: python-app
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    volumes:
      - ./app.py:/app/app.py    # Mount app.py from outside
    depends_on:
      - redis
    ports:
      - "9014:5000"
    command: ["python", "app.py"]